# Car2DB API credentials (required)
# Get your API key at: https://car2db.com/api/
# Demo key available at: https://car2db.com/api-token-demo/
CAR2DB_API_KEY=your_api_key_here

# Language for API responses (optional, default: en-US)
# Available: en-US, ru-RU, de-DE, es-ES, it-IT, pl-PL, fr-FR, da-DA, lv-LV, th-TH, zh-CN
CAR2DB_LANGUAGE=en-US

# MCP transport mode (optional, default: stdio)
# stdio - for Claude Desktop, Cursor (standard MCP)
# sse - for remote access via HTTP Server-Sent Events
MCP_TRANSPORT=stdio

# SSE mode port (optional, default: 3000)
# Only used when MCP_TRANSPORT=sse
MCP_SSE_PORT=3000
